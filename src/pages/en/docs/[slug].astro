---
import Base from '../../../layouts/Base.astro';
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const docs = await getCollection('docs')
  return docs
    .filter(d => d.id.startsWith('en/'))
    .map(d => ({ params: { slug: d.id.split('/').slice(1).join('/') } }))
}
---

<Base title="Docs">
  <meta http-equiv="refresh" content="0;url=https://docs.qomicex.top" />
  <div class="mx-auto max-w-lg px-4 py-20 text-center">
    <p class="text-muted-foreground">Docs have moved to <a href="https://docs.qomicex.top" class="text-primary underline underline-offset-2">docs.qomicex.top</a>.</p>
  </div>
</Base>
